#ifndef _SYNCHRONIZED_H__
#define _SYNCHRONIZED_H__

#include <vector>

void find_syncronize_sequence_trivial(const std::vector<std::vector<int>>
                                                &edge);

#endif
